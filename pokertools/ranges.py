from pokertools import Card


def parse_range(*patterns: str) -> set[set[Card]]:
    """Parses the supplied patterns.

    :param patterns: the supplied patterns to be parsed.
    :return: the parsed cards
    """
    raise NotImplementedError
